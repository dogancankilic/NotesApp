<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="20dp"
    android:id="@+id/addact">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <EditText
            android:id="@+id/etvTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"

            android:ems="10"
            android:hint="Başlık"
            android:inputType="textMultiLine"
            android:textColorLink="@android:color/black"
            android:textSize="22sp" />

        <EditText
            android:id="@+id/etvNote"
            android:layout_width="match_parent"
            android:layout_height="200dp"
            android:ems="10"
            android:hint="Ne düşünüyorsun"
            android:gravity="start"
            android:inputType="textMultiLine"
            android:textColorLink="@android:color/black"
            android:textSize="17sp" />
    </LinearLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <Space
            android:layout_width="596dp"
            android:layout_height="match_parent"
            android:layout_weight="1" />


        <ru.dimorinny.floatingtextbutton.FloatingTextButton
            android:id="@+id/img_add"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:floating_background_color="@color/greenish"
            app:floating_left_icon="@drawable/ic_save_black_24dp"
            app:floating_title="Kaydet"
            app:floating_title_color="@android:color/white" />
    </LinearLayout>

</LinearLayout>